<!DOCTYPE html>
<html>
<head>
  <title>Invoice</title>
  <style>
    body { font-family: Arial, sans-serif; margin: 0; padding: 20px; }
    .header, .footer { text-align: center; margin-bottom: 20px; }
    .addresses { margin-bottom: 30px; }
    .from, .to { width: 45%; display: inline-block; vertical-align: top; margin-bottom: 20px; }
    .from { margin-right: 5%; }
    .invoice-table { width: 100%; border-collapse: collapse; margin-top: 20px; }
    .invoice-table th, .invoice-table td { border: 1px solid #ddd; padding: 8px; }
    .invoice-table th { background-color: #f4f4f4; }
    .total { text-align: right; font-weight: bold; }
    .section-title { font-weight: bold; margin-bottom: 10px; }
  </style>
</head>
<body>
  <div class="header">
    <h1>Invoice</h1>
    <p>Invoice #<%= "#{@ticket.ticket_number}" %></p>
    <p>Date: <%= @ticket.created_at.strftime('%B %d, %Y') %></p>
  </div>

  <div class="addresses">
    <div class="from">
      <div class="section-title">From:</div>
      <p>
        {USER NAME}<br>
        {USER ADDRESS}<br>
        {USER PHONE}
      </p>
    </div>
    <div class="to">
      <div class="section-title">To:</div>
      <p>
        <%= @ticket.customer.name %><br>
        <%= @ticket.customer.address.gsub("\n", "<br>").html_safe %><br>
        <%= @ticket.customer.phone_number %>
      </p>
    </div>
  </div>

  <table class="invoice-table">
    <thead>
      <tr>
        <th>Description</th>
        <th>Amount</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>On-Site Fee</td>
        <td>$19.99</td>
      </tr>
      <tr>
        <td>Windows Reinstallation & Updates, Bios Update, & Driver Updates</td>
        <td>$79.99</td>
      </tr>
      <tr>
        <td>Windows 8.1 Pro Key</td>
        <td>$29.99</td>
      </tr>
      <tr>
        <td>PNY 64GB Flash Drive</td>
        <td>$7.99</td>
      </tr>
      <tr>
        <td>Tax</td>
        <td>$8.28</td>
      </tr>
    </tbody>
    <tfoot>
      <tr>
        <td class="total">Balance Due</td>
        <td class="total">$146.24</td>
      </tr>
    </tfoot>
  </table>

  <div class="footer">
    <p>You can make payment by calling {USER PHONE} or mailing cash or check to {USER ADDRESS}.</p>
    <p>Questions? Call {USER PHONE}</p>
  </div>
</body>
</html>
